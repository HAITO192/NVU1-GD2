## Cách khởi chạy
1. Copy đoạn văn bản muốn phân tích vào file 'text.txt' trong folder api
2. Vào folder có file 'docker-compose.yaml'
3. Dùng lệnh: docker compose up --build
4. Test API: mở terminal khác và dùng lệnh: curl http://localhost:5000/xu-ly-van-ban